1. **Sampling Fundamentals**
   * Definition of sampling
   * Why sampling is needed (approximating expectations and learning parameters)
   * Properties of samplers (unbiased estimators and variance approximation)
2. **Valid Samplers**
   * Univariate sampling
   * Multivariate sampling (naive approaches)
   * Ancestral sampling
   * The challenge of sampling with evidence 
3. **Approximate Samplers**
   * Gibbs Sampling (example, definition, properties, and tips)
   * Markov Chain Monte Carlo (MCMC) sampling framework and procedures
   * The MCMC convergence issue
4. **Sampling for Approximated Inference**
   * How to use sampling in a Bayesian Model
   * Latent Dirichlet Allocation (LDA) Gibbs Sampling procedure