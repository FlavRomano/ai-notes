---
date: 13-07-2025
reference: spark for java dev course
tags:
  - fleeting
  - task
  - spark
  - definition
---
When execution plan is running
- **driver** is responsible for sending across the network any Java functions that needs to be executed over the data
- when a **worker node** receives a function, it gets executed against the partitions.

We call a **task** a function (java code in this case) executing against a partition.