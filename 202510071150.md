---
date: 07-10-2025
reference: ML-7
tags:
  - fleeting
  - ml
  - neural-network
  - perceptron
  - proof
---
The perceptron learning algorithm is always able to learn what it is able to represent.
- perceptron convergence theorem
- can solve linearly separable problem.

The algorithm converge in a finite number of step, even if start randomly.

---

All false negative because we had all true positive before.
- with all simplified stuff, eta=1 and all positive patterns therefore $d=1$
- we sum only the misclassified input to the weights. $$w_{j} = w_{j-1} + x_{i_{j}}$$
- $q \alpha$ because we take $q$ steps the minimum $\alpha$
- The $x_{i_q}$, is $q$-th error, is negative (because we are building $w$ and it's build just with the error, not the correct classified input) so we delete the negative term $$2 w (q-1)x_{i_{q}}$$because we are building an upper bound.

The biggest diff. between perptron and lms is 
- lms changes the weights even if the pattern is well classified
- in order to get a decision boundary that gets prediction closer to true value.