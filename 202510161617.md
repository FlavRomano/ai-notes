---
date: 16-10-2025
reference: ML-9
tags:
  - fleeting
  - ml
  - neural-network
  - backpropagation
  - generalize-delta-rule
---
- Rule that works for all the network, not just one unit.

the partial derivative of net_t over w_tu is local on unit t.

we search delta value for all the units.

how each output unit influence the error

how
- changing the net -> change the error
	- it's the delta_k from the output layer
- changing the output -> change the net

what's the responsability of the unit j on the error?
- if delta_k is high => high error for the unit