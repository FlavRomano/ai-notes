---
date: 26-09-2025
reference: ML-5
tags:
  - fleeting
  - ml
  - classification
  - linear-models
---
- classification by hyperplanes
- state and formulate by LMS

categorical targets 
- e.g 0/1, -1/+1
- find hyperplane $$x^Tw = w^{T}x = 0$$
	- defines a decision boundary
	- just the sign provide the correct category $$h(x) = sign(w^Tx + w_{0})$$ linear threshold unit
		- threshold because we have the $w_0$ that rule the change of decision. $$h(x) = w^Tx + w_{0} \ge 0 \text{ equivalent to } h(x) = w^Tx \ge -w_{0} $$