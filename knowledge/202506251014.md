---
date: 25-06-2025
reference: MML - Analytic Geometry
tags:
  - fleeting
  - linearMapping
  - definition
---
Given a vector space $V$ and $\Omega:V\times V \to \mathbb R$ be a bilinear mapping that takes two vectors and maps them onto a real number
- $\Omega$ is called **symmetric** if $$\Omega(x,y)=\Omega(y,x)\quad\forall x,y\in V$$ in other words, **the order of the arguments does not matter**
- $\Omega$ is called **positive definite** if $$\forall x\in V \setminus \{0\} \mid \Omega (x,x) > 0,\quad \Omega(0,0) =0$$  in other words, **when you apply it to the same non-zero vector twice, you always get a positive number.**

When $\Omega$ is both positive definite and symmetric, it's called an **inner product on $V$**