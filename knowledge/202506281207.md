---
date: 28-06-2025
reference: MML - Probability
tags:
  - fleeting
  - joint-probability
  - definition
  - probability
  - discrete
---
Given two random variables $X:\Omega \to \mathcal{T}_{X}$, $Y:\Omega \to \mathcal{T}_{Y}$
- the target space of the joint probability is the Cartesian product of the target spaces of each of the random variables $$\mathcal{T}_{X,Y}= \mathcal{T}_{X} \times \mathcal{T}_{Y}$$

We define the **joint probability** as 
- the entry of both values jointly $$P(X=x_{i}, Y=y_{j}) = \frac{n_{ij}}{N}$$
- where 
	- $n_{ij}$ is the number of events with state $x_{i}$ and $y_{j}$ $$n_{ij}=\# \text{events with state } x_{i} \text{ and } y_{j}$$
	- $N$ is the total number of events $$N=\# events$$

![[Pasted image 20250628123444.png]]

The probability that $X=x$ and $Y=y$ is written as $p(x,y)$ and is called the **joint probability**

> Think of probability as a function that takes state $x$ and $y$ and returns a real number.