---
date: 21-09-2025
reference: CM - Lecture 2.1
tags:
  - fleeting
  - univariate
  - problem
  - optimization
  - constrained
---
We have to find the min/max of the function inside a feasible region.
We cannot go everywhere.
![[Pasted image 20250921120157.png]]
We'll going to look at solution with points inside the $X$ region.

$$(P)\quad f_{*} = min\{f(x) : x\in X\}$$
So now we want the smaller element of the image of $X$ through $f$
$$f_{*} = v(P) = min (im(X,f))$$ $$X_{*} = L(f,f_{*}) \cap X$$
